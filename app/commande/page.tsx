"use client";
import Link from "next/link";
import {useEffect,useState} from "react";
import {supabase} from "@/lib/supabase";
import {formatPrice} from "@/lib/format";
export default function Commande(){const [cart,setCart]=useState<any[]>([]);const [form,setForm]=useState({name:"",phone:"",address:"",notes:""});const [msg,setMsg]=useState("");useEffect(()=>{setCart(JSON.parse(localStorage.getItem("hurrah-cart")||"[]"))},[]);const total=cart.reduce((s,p)=>s+p.price*p.quantity,0);async function submit(e:React.FormEvent){e.preventDefault();setMsg("Envoi...");if(!cart.length){setMsg("Votre panier est vide.");return}const {data:order,error}=await supabase.from("orders").insert({customer_name:form.name,customer_phone:form.phone,customer_address:form.address,notes:form.notes,total}).select().single();if(error||!order){setMsg(error?.message||"Erreur.");return}const {error:itemError}=await supabase.from("order_items").insert(cart.map(p=>({order_id:order.id,product_id:p.id,product_name:p.name,quantity:p.quantity,price:p.price})));if(itemError){setMsg(itemError.message);return}localStorage.removeItem("hurrah-cart");setMsg("Commande enregistrée avec succès.");setCart([])}return <main className="min-h-screen bg-[#faf8f4] px-6 py-16"><div className="mx-auto max-w-3xl"><Link href="/panier" className="font-bold">← Panier</Link><h1 className="mt-8 text-5xl font-black">Finaliser la commande</h1><p className="mt-4">Total : <strong>{formatPrice(total)}</strong></p><form onSubmit={submit} className="mt-10 space-y-5 rounded-3xl bg-white p-8 shadow-sm"><input required className="w-full rounded-2xl border px-5 py-4" placeholder="Nom complet" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input required className="w-full rounded-2xl border px-5 py-4" placeholder="Téléphone" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/><input className="w-full rounded-2xl border px-5 py-4" placeholder="Adresse" value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/><textarea className="min-h-32 w-full rounded-2xl border px-5 py-4" placeholder="Notes" value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/><button className="w-full rounded-full bg-neutral-950 py-4 font-bold text-white">Confirmer la commande</button>{msg&&<p className="rounded-2xl bg-orange-50 p-4 font-semibold">{msg}</p>}</form></div></main>}
